# Pocket ID secrets. setup.sh generates these — you shouldn't need to edit.

# Key used to encrypt credentials at rest. 32+ random bytes, base64 or hex.
ENCRYPTION_KEY=

# Maintenance mode. Leave false after first boot.
MAINTENANCE_MODE=false
